node_modules
.env
.env.local
*.log
.DS_Store

# Hardhat (contracts added later)
artifacts/
cache-hardhat/
typechain-types/

# Private planning docs — never push
docs/SCOPE.*
docs/PITCH.*
docs/SCRIPT.*
*.pdf
*.mp4

# Derived indexer state — regenerable from chain
data/
deployments.local.json

# build artifact from the mascot generator
site/assets/_*.html
